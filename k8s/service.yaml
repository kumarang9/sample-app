apiVersion: v1
kind: Service

metadata:
  name: sample-app
  namespace: prod

spec:

  selector:
    app: sample-app

  ports:
  - port: 80
    targetPort: 5000

  type: LoadBalancer
